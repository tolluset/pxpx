# Favicon Update

- Added browser-facing favicon endpoints at `/favicon.svg` and `/favicon.ico` in the Cloudflare worker.
- Matched the icon palette to the terminal client colors: background `#020617`, panel `#0f172a`, border `#334155`, pixels `#f59e0b`, `#38bdf8`, `#10b981`, `#fb7185`.
- Stored the favicon source and ICO payload in `cloudflare/favicon.ts`.
